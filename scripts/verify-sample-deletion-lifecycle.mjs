import{readFileSync}from'node:fs';import{spawnSync as rawSpawnSync}from'node:child_process';
const roleSetup="drop extension if exists pg_graphql cascade;do $$begin if not exists(select 1 from pg_roles where rolname='supabase_storage_admin')then create role supabase_storage_admin;end if;if not exists(select 1 from pg_roles where rolname='authenticated')then create role authenticated;end if;if not exists(select 1 from pg_roles where rolname='anon')then create role anon;end if;if not exists(select 1 from pg_roles where rolname='service_role')then create role service_role;end if;end$$;create schema if not exists auth;create schema if not exists storage authorization supabase_storage_admin;create or replace function auth.uid()returns uuid language sql stable as $$select nullif(current_setting('request.jwt.claim.sub',true),'')::uuid$$;";
const spawnSync=(command,args,options)=>{const result=rawSpawnSync(command,args,options?.input?{...options,input:roleSetup+options.input}:options);if(result.status===0&&args?.includes('pg_isready'))Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,1000);return result;};
const name=`tenops-sample-deletion-${process.pid}`,image='postgres:17.6',admin='00000000-0000-0000-0000-000000000001',creator='00000000-0000-0000-0000-000000000002',other='00000000-0000-0000-0000-000000000003',inactive='00000000-0000-0000-0000-000000000004';
const migrations=['20260901_005_sample_color_plate_generator.sql','20260917_001_sample_formulation_foundation.sql','20260917_002_sample_draft_library_identity.sql','20260917_003_sample_deletion_lifecycle.sql'].map(file=>readFileSync(`supabase/migrations/${file}`,'utf8')).join('\n');
const setup=String.raw`create table public.app_users(user_id uuid primary key,display_name text,role text,is_active boolean);create table public.jobs(id uuid primary key,name text,customer text,job_number text);create table public.bids(id uuid primary key,customer text,project_name text);set role supabase_storage_admin;create table storage.buckets(id text primary key,name text,public boolean,file_size_limit bigint,allowed_mime_types text[]);create table storage.objects(bucket_id text,name text);grant all on storage.buckets,storage.objects to postgres;reset role;create function public.tenops_touch_updated_at()returns trigger language plpgsql as $$begin new.updated_at=clock_timestamp();return new;end$$;create function public.has_app_capability(p_capability text)returns boolean language sql stable security definer as $$select exists(select 1 from public.app_users where user_id=auth.uid() and is_active and role<>'guest')$$;create function public.require_app_capability(p_capability text)returns void language plpgsql security definer as $$begin if not public.has_app_capability(p_capability)then raise exception 'denied' using errcode='42501';end if;end$$;insert into public.app_users values('${admin}','Admin','admin',true),('${creator}','Creator','member',true),('${other}','Other','member',true),('${inactive}','Inactive','member',false);`;
const tests=String.raw`
select set_config('request.jwt.claim.sub','${creator}',false);create temp table ids(name text primary key,id uuid);
insert into ids values('plain',public.create_sample()),('versioned',public.create_sample()),('issued',public.create_sample());
insert into ids values('version',public.save_sample_working_version((select id from ids where name='versioned'),'checkpoint'));
insert into ids values('issued_version',public.save_sample_working_version((select id from ids where name='issued'),'issued checkpoint'));
insert into ids values('document',public.issue_sample_form((select id from ids where name='issued')));
update public.sample_issued_documents set storage_path=(select id::text||'/document.pdf' from ids where name='issued') where id=(select id from ids where name='document');
select public.permanently_delete_sample_draft((select id from ids where name='plain'),'PERMANENTLY_DELETE_SAMPLE_DRAFT');
select public.permanently_delete_sample_draft((select id from ids where name='versioned'),'PERMANENTLY_DELETE_SAMPLE_DRAFT');
do $$begin if exists(select 1 from public.samples where id in(select id from ids where name in('plain','versioned')))or exists(select 1 from public.sample_working_versions where id=(select id from ids where name='version'))then raise exception 'Draft-owned residue remains';end if;begin perform public.permanently_delete_sample_draft((select id from ids where name='issued'),'PERMANENTLY_DELETE_SAMPLE_DRAFT');raise exception 'Creator deleted issued Sample';exception when object_not_in_prerequisite_state then null;end;begin delete from public.sample_working_versions where id=(select id from ids where name='issued_version');raise exception 'Direct version delete allowed';exception when object_not_in_prerequisite_state then null;end;begin update public.sample_issued_documents set issued_snapshot='{}' where id=(select id from ids where name='document');raise exception 'Issued snapshot mutation allowed';exception when object_not_in_prerequisite_state then null;end;end$$;
select set_config('request.jwt.claim.sub','${other}',false);insert into ids values('other_test',public.create_sample());select set_config('request.jwt.claim.sub','${creator}',false);do $$begin perform public.permanently_delete_sample_draft((select id from ids where name='other_test'),'PERMANENTLY_DELETE_SAMPLE_DRAFT');raise exception 'Non-creator deleted Draft';exception when insufficient_privilege then null;end$$;
select set_config('request.jwt.claim.sub','${other}',false);do $$begin perform public.prepare_admin_delete_sample((select id from ids where name='issued'));raise exception 'Ordinary user prepared issued deletion';exception when insufficient_privilege then null;end$$;
select set_config('request.jwt.claim.sub','${admin}',false);do $$begin if not exists(select 1 from public.prepare_admin_delete_sample((select id from ids where name='issued')) where storage_bucket='sample-documents' and storage_path<>'')then raise exception 'Owned PDF was not prepared';end if;end$$;update public.sample_issued_documents set storage_path='' where sample_id=(select id from ids where name='issued');select public.admin_permanently_delete_sample((select id from ids where name='issued'),'PERMANENTLY_DELETE_ISSUED_SAMPLE');
do $$begin if exists(select 1 from public.samples where id=(select id from ids where name='issued'))or exists(select 1 from public.sample_working_versions where sample_id=(select id from ids where name='issued'))or exists(select 1 from public.sample_issued_documents where sample_id=(select id from ids where name='issued'))or exists(select 1 from public.sample_blend_rows where sample_id=(select id from ids where name='issued'))then raise exception 'Admin deletion residue remains';end if;end$$;
select set_config('request.jwt.claim.sub','${inactive}',false);do $$begin perform public.prepare_admin_delete_sample((select id from ids where name='other_test'));raise exception 'Inactive user allowed';exception when insufficient_privilege then null;end$$;select set_config('request.jwt.claim.sub','',false);do $$begin perform public.permanently_delete_sample_draft((select id from ids where name='other_test'),'PERMANENTLY_DELETE_SAMPLE_DRAFT');raise exception 'Anonymous user allowed';exception when insufficient_privilege then null;end$$;
select 'sample deletion lifecycle verifier passed';`;
function run(args,input){const result=spawnSync('docker',args,{input,encoding:'utf8'});if(result.status!==0)throw new Error(`${result.stdout}\n${result.stderr}`);return result.stdout;}try{run(['run','--rm','-d','--name',name,'-e','POSTGRES_PASSWORD=postgres',image]);for(let attempt=0;attempt<40;attempt++){if(spawnSync('docker',['exec',name,'pg_isready','-U','postgres']).status===0)break;Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,250);if(attempt===39)throw new Error('Disposable PostgreSQL did not become ready.');}run(['exec','-i',name,'psql','-U','postgres','-d','postgres','-v','ON_ERROR_STOP=1'],setup+migrations+tests);console.log('Sample deletion lifecycle checks passed.');}finally{spawnSync('docker',['stop',name]);}
