import{readFileSync}from'node:fs';import{spawnSync}from'node:child_process';
const migration=readFileSync('supabase/migrations/20260902_001_sample_draft_deletion.sql','utf8'),name=`tenops-sample-delete-${process.pid}`,image='public.ecr.aws/supabase/postgres:17.6.1.143',member='00000000-0000-0000-0000-000000000002';
const setup=`create table public.app_users(user_id uuid primary key,is_active boolean);create table public.bids(id uuid primary key);create table public.jobs(id uuid primary key);create table public.vendor_catalog(id text primary key);create table public.samples(id uuid primary key,bid_id uuid references public.bids(id),job_id uuid references public.jobs(id),created_by_user_id uuid);create table public.sample_blend_rows(id uuid primary key,sample_id uuid not null references public.samples(id) on delete cascade,catalog_item_id text);create table public.sample_issued_documents(id uuid primary key,sample_id uuid not null references public.samples(id) on delete restrict);create function public.has_app_capability(p text)returns boolean language sql stable as $$select exists(select 1 from public.app_users where user_id=auth.uid() and is_active)$$;create function public.require_app_capability(p text)returns void language plpgsql as $$begin if not public.has_app_capability(p)then raise exception 'denied' using errcode='42501';end if;end$$;insert into public.app_users values('${member}',true);insert into public.bids values('10000000-0000-0000-0000-000000000001');insert into public.jobs values('20000000-0000-0000-0000-000000000001');insert into public.vendor_catalog values('catalog-1');insert into public.samples values('30000000-0000-0000-0000-000000000001','10000000-0000-0000-0000-000000000001','20000000-0000-0000-0000-000000000001','${member}'),('30000000-0000-0000-0000-000000000002',null,null,'${member}');insert into public.sample_blend_rows values('40000000-0000-0000-0000-000000000001','30000000-0000-0000-0000-000000000001','catalog-1'),('40000000-0000-0000-0000-000000000002','30000000-0000-0000-0000-000000000001',null),('40000000-0000-0000-0000-000000000003','30000000-0000-0000-0000-000000000002',null);insert into public.sample_issued_documents values('50000000-0000-0000-0000-000000000001','30000000-0000-0000-0000-000000000002');`;
const tests=`select set_config('request.jwt.claim.sub','${member}',false);do $$begin begin perform public.permanently_delete_sample_draft('30000000-0000-0000-0000-000000000001','WRONG');raise exception 'Invalid confirmation allowed';exception when invalid_parameter_value then null;end;end$$;select public.permanently_delete_sample_draft('30000000-0000-0000-0000-000000000001','PERMANENTLY_DELETE_SAMPLE_DRAFT');do $$begin if exists(select 1 from public.samples where id='30000000-0000-0000-0000-000000000001')or exists(select 1 from public.sample_blend_rows where sample_id='30000000-0000-0000-0000-000000000001')then raise exception 'Draft or dependent material rows remain';end if;if(select count(*) from public.bids)<>1 or(select count(*) from public.jobs)<>1 or(select count(*) from public.vendor_catalog)<>1 then raise exception 'Unrelated canonical record changed';end if;begin perform public.permanently_delete_sample_draft('30000000-0000-0000-0000-000000000002','PERMANENTLY_DELETE_SAMPLE_DRAFT');raise exception 'Issued Sample deletion allowed';exception when object_not_in_prerequisite_state then null;end;if not exists(select 1 from public.samples where id='30000000-0000-0000-0000-000000000002')or not exists(select 1 from public.sample_issued_documents where sample_id='30000000-0000-0000-0000-000000000002')then raise exception 'Issued Sample history changed';end if;end$$;`;
function run(args,input){const result=spawnSync('docker',args,{input,encoding:'utf8'});if(result.status!==0)throw new Error(`${result.stdout}\n${result.stderr}`);return result.stdout;}try{run(['run','--rm','-d','--name',name,'-e','POSTGRES_PASSWORD=postgres',image]);for(let attempt=0;attempt<40;attempt++){if(spawnSync('docker',['exec',name,'pg_isready','-U','postgres']).status===0)break;Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,250);if(attempt===39)throw new Error('Disposable PostgreSQL did not become ready.');}run(['exec','-i',name,'psql','-U','postgres','-d','postgres','-v','ON_ERROR_STOP=1'],setup+migration+tests);console.log('Sample draft deletion checks passed.');}finally{spawnSync('docker',['stop',name]);}
