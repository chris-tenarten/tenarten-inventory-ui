import { moneyToCents } from './calculations'; import type { PurchaseOrderDraft } from './types';
export function validatePurchaseOrderDraft(draft: PurchaseOrderDraft): string[] { const errors:string[]=[]; if(!draft.vendorNameSnapshot.trim()) errors.push('Vendor is required.'); if(!draft.orderDate) errors.push('PO Date is required.'); if(!draft.createdBy.trim()) errors.push('PO Originated By is required.'); if(!draft.lines.length) errors.push('Add at least one chip line.'); draft.lines.forEach((line,index)=>{ const label=`Line ${index+1}`; if(!line.details.materialNameSnapshot.trim()) errors.push(`${label}: material is required.`); if(!line.details.chipSize.trim()) errors.push(`${label}: chip size is required.`); if(!(Number(line.details.quantityOrdered)>0)) errors.push(`${label}: quantity ordered must be positive.`); if(line.details.packageQuantity && !(Number(line.details.packageQuantity)>0)) errors.push(`${label}: package quantity must be positive.`); if(!line.details.orderUnit.trim()) errors.push(`${label}: order unit is required.`); if(line.details.unitPrice && moneyToCents(line.details.unitPrice)===null) errors.push(`${label}: unit price is invalid.`); });for(const [label,value] of [['Discount',draft.discountPercent],['Tax',draft.taxPercent]] as const){if(value.trim()&&(!/^\d+(\.\d+)?$/.test(value)||Number(value)<0||Number(value)>100))errors.push(`${label} percent must be between 0 and 100.`);}if(draft.freight.trim()&&moneyToCents(draft.freight)===null)errors.push('Freight must be a non-negative currency amount.');return errors; }
