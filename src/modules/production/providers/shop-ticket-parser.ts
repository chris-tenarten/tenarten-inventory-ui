import type { ParsedFields } from './parser-utils';

// The observed CAD ticket title block includes identifiers owned by several
// project parties. Recognition is useful, but none are promoted without an
// explicit Tenarten-owned label.
export function parseShopTicket(): ParsedFields {
  return {};
}
