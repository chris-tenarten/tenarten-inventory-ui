export type ProposalStatus='draft'|'issued';
export type ProposalLineType='product'|'charge'|'informational'|'included';
export type ProposalLine={id:string;lineType:ProposalLineType;itemNumber:string;description:string;ref:string;colorPlate:string;quantity:string;unit:string;length:string;width:string;heightThickness:string;cft:string;lf:string;estimatedWeight:string;rate:string;total:string;sourceMetadata:Record<string,unknown>;displayOrder:number};
export type Proposal={id:string;jobId:string;lineageId:string;priorProposalId:string;estimateBase:string;versionMajor:number;versionMinor:number;estimateNumber:string;status:ProposalStatus;proposalDate:string;customerName:string;customerAddress:string;customerContact:string;projectName:string;projectNumber:string;projectLocation:string;sideMark:string;salesRep:string;terms:string;validDays:string;requestedDelivery:string;fob:string;destinationZip:string;taxCounty:string;freightEstimate:string;preliminaryDrawingsAttached:boolean;cratingIncluded:boolean;cutTicketsIncluded:boolean;fieldDimensioningExcluded:boolean;submittedByName:string;submittedByPhone:string;submittedByEmail:string;notes:string;disclaimerSnapshot:string;formulaSnapshot:string;fieldSources:Record<string,DrawingFieldEvidence>;taxEnabled:boolean;taxRate:string;subtotal:string;tax:string;total:string;createdAt:string;updatedAt:string;issuedAt:string;creatorName:string;lines:ProposalLine[]};
export type DrawingFieldEvidence={value:string;sourceFile:string;jobAttachmentId:string;sourceLabel:string;confidence:'exact-label'};
export type DrawingSuggestionKey='projectName'|'projectLocation'|'projectNumber'|'customer'|'item'|'ref'|'colorPlate'|'matrix'|'finish'|'quantity'|'unit'|'description';
export type DrawingSuggestions={fields:Partial<Record<DrawingSuggestionKey,DrawingFieldEvidence>>;sourceFile:string};
export const PROPOSAL_DISCLAIMER=`Unit sizes listed are for reference only. Final fabrication dimensions and field measurements shall be provided by purchaser prior to initiating fabrication. TENARTEN reserves the right to reasonably adjust final price due to variances in sizes and intent from this quote.\n\nPrice is based on 100% standard domestic marble chips. Imported, exotic, glass, plastic, granite, shell and recycled materials are not included unless noted otherwise.\n\nLead time for materials and fabrication durations will be confirmed upon receipt of notice to proceed. Freight arrangements and cost are by customer unless noted otherwise.`;
